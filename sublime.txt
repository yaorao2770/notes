1. jslint检查

	1. 安装 SublimeLinter
	2. 安装 SublimeLinter-jshint
	3. npm install -g jshint
	   jshint -v
	4. 重启sublime

2. editorConfig

	项目根目录设置 .editorconfig 

	# Editor configuration, see http://editorconfig.org
	root = true

	[*]
	charset = utf-8
	indent_style = tab
	indent_size = 4
	insert_final_newline = true
	trim_trailing_whitespace = true

	[*.md]
	max_line_length = off
	trim_trailing_whitespace = false

	[*.ts]
	indent_size = 4

3. 自定义快捷键

	perferences -> key bindings 

	[

	    //chrome
	    { "keys": ["alt+z"], "command": "side_bar_files_open_with",
	            "args": {
	                "paths": [],
	                "application": "C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe",
	                "extensions":".*"
	            }
	    },
	    { "keys": ["ctrl+1"], "command": "build" },
	    { "keys": ["ctrl+shift+g"], "command": "reindent" }
	]